# Rust build output
/target
/dist

# coverage output
coverage.txt
lcov.info

# rustfmt backups
**/*.rs.bk

# macOS
.DS_Store

# Keychain files. The tests create these under $TMPDIR and clean up after
# themselves, but a stray one must never be committed.
*.keychain
*.keychain-db
